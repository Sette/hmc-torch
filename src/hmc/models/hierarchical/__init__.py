"""Hierarchical model components for HMC.

Provides reusable classification heads, hierarchical losses, label-graph
encoders, post-processing (reconciliation, calibration), and sparse R-matrix
approximations for large label spaces (e.g., Gene Ontology).
"""
